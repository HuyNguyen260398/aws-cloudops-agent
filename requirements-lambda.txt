# Lambda-optimized requirements
# Only essential dependencies for AWS Lambda deployment

# Core dependencies
boto3>=1.34.0
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# Note: 'rich' is excluded for Lambda as it's primarily for CLI interfaces
# If you need rich formatting in Lambda responses, uncomment the line below:
# rich>=13.0.0